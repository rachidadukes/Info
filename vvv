private static void PrintBlock(string text, Color color)
{
    var styled = new Text(text, new Style(foreground: color));
    AnsiConsole.Write(styled);          // preserves all newlines/indentation
    AnsiConsole.WriteLine();            // trailing newline
}
