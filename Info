SELECT 
    AccountNumber,
    AccessNumber,
    AccountType,
    Environment,
    COUNT(*) AS DuplicateCount
FROM AccessAcctNumbers
GROUP BY 
    AccountNumber, 
    AccessNumber, 
    AccountType, 
    Environment
HAVING COUNT(*) > 1
ORDER BY AccountNumber;
