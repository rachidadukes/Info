// TextBox
var textbox = new TextBox
{
    Width = 200,
    Margin = new Thickness(0, 5, 0, 5),
    Tag = name
};

// ✅ Pre-fill known values
if (name == "EmployeeId")
    textbox.Text = pv.UserId; // 👈 use your actual variable/property name here
else if (name == "PCName")
    textbox.Text = Environment.MachineName;

Grid.SetRow(textbox, row);
Grid.SetColumn(textbox, 1);
NewDataFormGrid.Children.Add(label);
NewDataFormGrid.Children.Add(textbox);
