SELECT *
FROM AccessAcctNumbers
WHERE (AccountNumber, AccessNumber, AccountType, Environment) IN (
    SELECT 
        AccountNumber,
        AccessNumber,
        AccountType,
        Environment
    FROM AccessAcctNumbers
    GROUP BY 
        AccountNumber,
        AccessNumber,
        AccountType,
        Environment
    HAVING COUNT(*) > 1
)
ORDER BY AccountNumber;
