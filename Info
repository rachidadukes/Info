public List<AccountModel> ReadAcctListDetail(RequestResponseLogModel log)
{
    var accountsList = new List<AccountModel>();
    var xml = log.FullMessage;

    var xmlDoc = new XmlDocument();
    xml.LoadXml(xml);

    var acctProfiles = xmlDoc.GetElementsByTagName("AcctProfile");

    foreach (XmlNode profileNode in acctProfiles)
    {
        var acctIdNode = profileNode.SelectSingleNode("AcctKeys/AcctId");
        var acctTypeNode = profileNode.SelectSingleNode("AcctKeys/AcctType");

        if (acctIdNode != null && acctTypeNode != null)
        {
            var model = new AccountModel
            {
                EventID = log.EventID,
                Timestamp = log.DateTimeStamp,
                AccountNumber = acctIdNode.InnerText,
                AcctType = acctTypeNode.InnerText
            };

            accountsList.Add(model);
        }
    }

    return accountsList;
}
