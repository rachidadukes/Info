WITH Ranked AS (
    SELECT 
        Id,
        AccountNumber,
        AccessNumber,
        AccountType,
        Environment,
        ROW_NUMBER() OVER (
            PARTITION BY AccountNumber, AccessNumber, AccountType, Environment
            ORDER BY Id
        ) AS RowNum
    FROM AccessAcctNumbers
)
SELECT 
    Id,
    AccountNumber,
    AccessNumber,
    AccountType,
    Environment
FROM Ranked
WHERE RowNum > 1
ORDER BY AccountNumber;
