using ApiClientLib;

public static async Task<int> CallApi()
{
    var request = RequestBuilder.BuildXmlRequest();

    var client = new ApiXmlClient();                 // <-- no URL passed
    var response = await client.SendAsync(request);  // library reads config

    Console.WriteLine(response);
    return 0;
}
