Dim cert As X509Certificate2 = FindValidCertificateBySubjectName(matches, subject)

            If cert Is Nothing Then
                Throw New InvalidOperationException($"No valid client-auth certificate found for SubjectName contains '{subject}'.")
            End If
