for (int i = 0; i < toShow.Count; i += columns)
{
    var row = toShow.Skip(i).Take(columns).ToList();
    var cells = row.Select(s => s ?? string.Empty).ToList();
    while (cells.Count < columns)
        cells.Add(string.Empty);

    grid.AddRow(cells.ToArray());
}
