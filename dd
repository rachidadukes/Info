var envChoices = new List<string> { "UNIT", "INTG" };
var selectedEnvironment = "UNIT";
var sysResponses = new List<string> { "Environment initialized", "DB connection ready" };

AppLogger.Summary.Add(envChoices, selectedEnvironment, sysResponses);
