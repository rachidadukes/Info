var httpResult = await responseObject.ReadXMLresponse(SoapXMLRequest, endpoint);

if (!httpResult.IsSuccess)
{
    Console.WriteLine(httpResult.ErrorMessage);
    return null; // or handle the error case
}

originalXMLResponse = httpResult.Body;
