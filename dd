 var repo = new ApiCacheRepository("Data Source=SqliteMember.db;Cache=Shared");

        // Only run seeding if you pass "seed" as an argument
        if (args.Length > 0 && args[0] == "seed")
        {
            await ApiCacheSeeder.SeedAsync(repo);
            return;
        }
