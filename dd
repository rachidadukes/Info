all = await dbContext.AccessAcctNumbers
    .OrderBy(e => e.Environment)                 // sort by Environment first
    .ThenByDescending(e => e.AccessNumber)       // then by AccessNumber
    .GroupBy(e => new { e.AccessNumber, e.Environment })
    .Select(g => g.First())
    .ToListAsync();
