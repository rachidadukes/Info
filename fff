HttpResponseMessage resp = null;
try
{
    resp = await client.PostAsync(endpointUrl, content);   // <— failing here
}
catch (Exception ex)
{
    // Return the deepest details (WebException/SChannel/Socket, etc.)
    return "SEND FAIL\n" + DiagnosticsUtil.ExceptionToString(ex);
}
