if (cert is X509Certificate2 c2)
{
    var san = c2.Extensions["2.5.29.17"];
    if (san is not null)
    {
        Console.WriteLine("[TLS] ServerCert SANs:");
        Console.WriteLine("    " + san.Format(true).Trim());
    }
}
