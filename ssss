catch (Exception ex)
{
    var panel = new Panel($"[white]{ex.Message}[/]")
        .Header("[bold red]❌ Certificate Health Check • Failed[/]")
        .Border(BoxBorder.Rounded);
    AnsiConsole.Write(panel);

    return null;
}
